--index-url https://download.pytorch.org/whl/cu126
torch
--extra-index-url https://pypi.org/simple
numpy
tqdm
pyyaml
scikit-learn
matplotlib

# Optional optimized PointNet++ backend:
# Install PyTorch3D separately with a wheel matching the local Torch/CUDA build.
